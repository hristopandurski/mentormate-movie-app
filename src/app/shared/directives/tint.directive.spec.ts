import { TintDirective } from './tint.directive';

describe('TintDirective', () => {
  it('should create an instance', () => {
    const directive = new TintDirective();
    expect(directive).toBeTruthy();
  });
});
