import { MaxCharacterCountDirective } from './max-character-count.directive';

describe('MaxCharacterCountDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxCharacterCountDirective();
    expect(directive).toBeTruthy();
  });
});
