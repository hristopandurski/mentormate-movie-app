import { ColorExtractorDirective } from './color-extractor.directive';

describe('ColorExtractorDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorExtractorDirective();
    expect(directive).toBeTruthy();
  });
});
